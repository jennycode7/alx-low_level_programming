#include <stdio.h>
#include <stdlib.h>

/**
 * main - adds integer
 * @argc: argument count
 * @argv: argument vector
 * Return: always 0
 */

int main(int argc, char *argc[])
{

	if (argc < 1)
	{
		printf("Erro\n");
		return (1);
	}
}

#include <stdio.h>
#include "main.h"

/**
 * add - adds two integer
 * @x: first integer
 * @i: second integer
 * Return: return addition
 */
int add(int x, int i)
{
	int num;

	num = x + i;
	return (num);
}
